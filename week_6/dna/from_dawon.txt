i = 0
    while i < len(dnaSequence):
        testString = dnaSequence[i:i+len(pattern)]
        if pattern == testString:
            repetitions += 1
            i += len(pattern)
        else:
            if repetitions > highestRepetition:
                highestRepetition = repetitions
            repetitions = 0
            i += 1
    

    return highestRepetition  